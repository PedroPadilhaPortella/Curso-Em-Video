# from math import comb
import math
import random
print(random.randint(10, 30))

# math.e => euler
# math.pi => pi
# math.tau => tau
# math.inf => ponto flutuante infinito positivo
# math.nan => ponto flutuante não numérico

# math.acos => arco de cosseno em radianos
# math.acosh => cosseno hiperbólico inverso de x
# math.asin => arco de seno em radianos
# math.asinh => seno hiperbólico inverso de x
# math.atan => arco de tangente em radianos
# math.atan2
# math.atanh => tangente hiperbólica inversa de x
# math.ceil(n) => arredonda pra cima
# math.comb(n, m) => combinação entre uma quantidade de elementos tomados de n em n
# math.copysign(x, y) => retorna um ponto flutuante com valor absoluto x mas sinal de y
# math.cos => cosseno
# math.cosh => cosseno hiperbólico de x
# math.degrees
# math.dist(p, q) => retorna a distancia euclidiana de dois pontos p e q
# math.erf
# math.erfc
# math.exp(x) => retorna euler elevado a x
# math.expm1(x) => retorna euler elevado a x - 1
# math.fabs(n) => retorna o valor absoluto de n
# math.factorial(n) => retorna um fatorial de um inteiro
# math.floor(n) => arredonda pra baixo
# math.fmod(n, m) => retorna a subtracao entre as casas decimais dos valores
# math.frexp() => sla pra que serve
# math.fsum([n, n, n, n, n]) => retorna a soma mais exata de valores flutuantes
# math.gamma() =>
# math.gcd(n, m) => retorna o mdc maior divisor comum entre dois valores
# math.hypot => hipotenusa
# math.isclose(n, m, rel-tol, abs_tol) => retorna um bool se dois valores são matematicamente proximos, que recebe uma tolerancia relativa e uma tolerancia absoluta dessa proximidade de valores
# math.isfinite(x) => retorna true se um valor não for infinito e numérico e falso se for NaN ou Infinity
# math.isinf(x) => se é infinito
# math.isnan(x) => se não é numerico
# math.isqrt()
# math.ldexp
# math.lgamma
# math.log() => logaritmo natural base e
# math.log10 => logaritmo na base 10
# math.log1p => logaritmo de 1+ x na base e
# math.log2 => logaritmo na abase 2
# math.modf(n) => retorna as partes fracionárias
# math.perm()=> permutacao entre uma quantidade de elementos tomados de n em n
# math.pow(n, 2) => potencia
# math.prod() => produto
# math.radians
# math.remainder
# math.sin => seno
# math.sinh => seno hiperbólico de x
# math.sqrt() => raiz quadrada
# math.tan => tangente
# math.tanh => tangente hiperbólica de x
# math.trunc(n) => trunca um float para int